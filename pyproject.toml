[project]
name = "ltlab-website-dev"
version = "0.1.0"
description = "Local validation tooling for the Language Technologies Lab website"
requires-python = ">=3.11"
dependencies = [
  "pyyaml>=6.0.2,<7",
]

[tool.uv]
package = false
